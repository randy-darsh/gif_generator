<% @categories.each do |category| %>
  <ul>
    <%= link_to category.name, category_path(category) %>
    <%= link_to "Delete Category", category_path(category), method: :delete if current_user.admin? %>
  </ul>
<% end %>
